## Reopen the saved team

1. Save your edits and reopen the same team. Complete any separate save action shown before leaving it.
2. Check that the request in **Start → trip** and the added instruction in **Trip Planner → System** are still present.
3. For future use, sign in with the same account, open the team, change **Start → trip → Value** and select **Run**.

## Optional: export a file copy

Right-click the team's name in the left sidebar and select **Export** to keep a file copy. [Sim's official import and export guide](https://docs.sim.ai/quick-reference)

The exported file contains the team configuration. Future model use still requires an available account and API connection; the file does not provide free usage allowances.

**Before sharing:** The provided template contains a reference to a saved key, not an actual API key. If you entered a key or personal information during the exercise, check whether it appears in the exported file and remove sensitive content before sharing.

## Continue using your team

Try the same team with different interests or travel conditions. The key practice is to **divide roles, pass results between agents and refine instructions to improve the output**. Review the final result yourself before using it.
