Use **Starter v0.2** for this exercise. If you previously imported an older version, import the new file as a separate workflow; you do not need to delete your existing team.

A **template** is a prepared example file containing roles and connections. This template arranges the sightseeing, food and itinerary specialists to work in sequence.

A **workflow** connects tasks in an execution sequence.

## Import the template

<a class="download" href="{{ '/assets/templates/hong-kong-day-trip-team-starter-v0.2.json' | relative_url }}" download>Download template<span>JSON · v0.2</span></a>

<ol>
<li markdown="1">
Select **Download template** to save the file.

<details markdown="1">
<summary>Download not working? Create the file with copy and paste</summary>

Select **Copy template contents**. If the button does not work, click inside the text box and use Ctrl+A → Ctrl+C (Windows) or ⌘A → ⌘C (Mac).

{% include template-copy.html %}

1. **Windows:** Open a new document in Notepad and paste the contents. In **Save As**, select **All files** as the file type and **UTF-8** as the encoding.
   **Mac:** Open a new document in TextEdit, select **Format → Make Plain Text**, then paste the contents.
2. Save as `hong-kong-day-trip-team-starter-v0.2.json`. Check that the name does not end in `.txt`. If your Mac asks which extension to use, choose **Use .json**.

Do not paste into Sim's chat or an individual AI block. Keep your API key in Sim's Secrets, not in this file.

After saving, continue with item 2 below. If you cannot save the file, ask a facilitator for help.

</details>
</li>
<li markdown="1">
Check that the file is saved as `hong-kong-day-trip-team-starter-v0.2.json`. **JSON** is the file format; you do not need to edit its contents.
</li>
<li markdown="1">
Find the **Workflows** heading in Sim's left sidebar.
</li>
<li markdown="1">
Select **··· → Import** beside that heading.
</li>
<li markdown="1">
Select the saved JSON file.
</li>
<li markdown="1">
Open **Hong Kong Day Trip Team - Starter v0.2** from the workflow list.
</li>
</ol>

## Check the team layout

Each box on the screen is called a **block**. Check that these four blocks are connected in order.

| Block name | Role |
| --- | --- |
| Start | Holds the travel request and activity information |
| Sightseeing Planner | Selects sightseeing activities and their areas |
| Food Planner | Selects food examples matching those areas and preferences |
| Trip Planner | Combines sightseeing, a food example and rest into an itinerary |

A **model** is the AI that generates an answer. Keep the model selected in the template for this exercise.

## About the supplied cards

Start's places field contains sightseeing cards and fictional food cards. Each has an ID and area so you can trace how the food specialist uses the sightseeing selection.

The short sightseeing descriptions and area labels draw on the Hong Kong Tourism Board's [Tsim Sha Tsui attractions guide](https://www.discoverhongkong.com/eng/attractions/top-things-to-see-and-do-around-tsim-sha-tsui-promenade.html) and [Art Park guide](https://www.discoverhongkong.com/eng/place-to-go/travel.guide-art-park.html). The food cards are teaching examples, not restaurant information from these sources.
